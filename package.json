{
  "name": "signal-ai",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev":      "node scripts/run.js dev",
    "start":    "node scripts/run.js start",
    "build":    "node scripts/run.js build",
    "lint":     "node scripts/run.js lint",
    "api":      "node scripts/run.js api",
    "api:prod": "node scripts/run.js api:prod"
  },
  "engines": {
    "node": ">=22.17.1"
  },
  "overrides": {
    "postcss": "^8.5.10",
    "next": {
      "postcss": "^8.5.10"
    }
  }
}
