node_modules
.next
dist
build
# .env is committed (safe defaults for all non-remote machines)
# .env.prod is NOT committed (production credentials)
.env.prod
.env.local
.env.*.local
*.log
.DS_Store
.venv
__pycache__
*.pyc
.pytest_cache
.turbo
coverage
.idea

# Log files — keep the folder, ignore log content
logs/*.log
logs/*.log.*
