node_modules
.next
dist
build
# Both env files contain credentials — never commit them
# Copy .env.example to .env after cloning and fill in your values
.env
.env.prod
.env.local
.env.*.local
*.log
.DS_Store
.venv
__pycache__
*.pyc
.pytest_cache
.turbo
coverage
.idea

# Log files — keep the folder, ignore log content
logs/*.log
logs/*.log.*
